# Copy this file to .env and fill in real values before running the app.
# No environment variables are currently required to build or run this
# project. Add entries here if you wire up analytics, a contact-form
# endpoint, etc. — never commit the real .env file.
#
# VITE_EXAMPLE_KEY=[PLACEHOLDER: description of what this value is]
